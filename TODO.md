# TODO

- [ ] Inspect `useCountries` implementation if duplicate/extra country fetches are caused by keying/memoization.
- [ ] Update `src/hooks/useAuthStats.js` to handle `/reviews/stats` 500 more gracefully (avoid noisy error state; keep defaults).
- [ ] Re-test the footer / auth modal stats loading paths in the browser console.
- [ ] Confirm no regressions in country fetching and UI rendering.

